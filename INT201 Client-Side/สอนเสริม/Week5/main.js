const insect = ["Bettle", "Ant", "Butterfly", "Scopion"]

// insect.forEach(
//   (element, index, array) => {
//     consoleIt(index + " : " + element)
//     consoleIt(array)
//   }
// )

// insect.splice(0, 2, "Antlion", "Worm")
//   (startAt, deleteCount, value)
//   consoleIt(insect)


function consoleIt(anything) {
  console.log(anything)
}